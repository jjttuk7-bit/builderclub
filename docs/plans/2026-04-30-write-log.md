# Build Log Write Page Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a standalone build-log writing screen connected from the builder room.

**Architecture:** Keep existing static pages intact. Create `write-log.html` as a form-first static prototype and link it from `builder-room.html`. Verify the expected form fields and navigation with a Node structure test.

**Tech Stack:** Static HTML, CSS, Node.js built-in assertions.

---

### Task 1: Structure Check

**Files:**
- Create: `tests/write-log-structure.test.mjs`

**Steps:**
1. Assert `write-log.html` exists.
2. Assert the page contains the required build-log fields.
3. Assert `builder-room.html` links to `write-log.html`.

### Task 2: Write Page

**Files:**
- Create: `write-log.html`
- Modify: `builder-room.html`

**Steps:**
1. Build a form-first layout, not a landing page.
2. Include fields for today's work, blocked issue, attempted solution, solved method, AI prompt, learned point, next action, and share scope.
3. Include navigation back to the builder room and dashboard.

### Task 3: Verification

**Steps:**
1. Run `node tests/write-log-structure.test.mjs`.
2. Run `node tests/builder-room-structure.test.mjs`.
3. Run `node tests/dashboard-structure.test.mjs`.
